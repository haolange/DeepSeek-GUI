- IPC 处理器统一使用 Zod Schema 进行入参校验，确保主进程接收数据的类型安全。
- 渲染层组件广泛采用 React.lazy 配合 Suspense 实现路由级和模块级的代码分割与懒加载。
- 业务逻辑与 UI 解耦，复杂的异步操作（如文件监听、API 请求）封装在主进程 Services 或渲染层 Store Actions 中。
- 配置文件与用户数据采用版本化结构（如 `AppSettingsV1`），并通过 `normalizeAppSettings` 进行迁移与默认值填充。