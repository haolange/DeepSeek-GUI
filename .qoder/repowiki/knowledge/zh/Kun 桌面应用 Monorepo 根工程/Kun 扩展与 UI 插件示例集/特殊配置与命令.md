需先在仓库根执行 `npm ci && npm run build --workspace @kun/extension-api && npm run build:kun`，再通过 `npm --prefix examples/extensions/<pkg> run validate|pack` 调用 `run-repository-kun-cli.mjs` 解析本地 `kun/dist/cli/serve-entry.js` 完成校验与打包；`kun-video-editor` 额外要求先运行 `npm run fixture:check` 以校验 fixtures 一致性。