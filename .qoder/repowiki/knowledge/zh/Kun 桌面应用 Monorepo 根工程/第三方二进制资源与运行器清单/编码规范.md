- 每个平台子目录通过独立的 `runner.json` / `manifest.json` 声明该平台的二进制来源、版本 ref、checksum 与架构标识，便于按平台维度维护。
- 所有远程二进制均提供 SHA256 校验值（asset.sha256 或 sourceSha256），用于下载后的完整性验证。
- 平台标识采用统一键名（darwin-arm64、darwin-x64、linux-x64、win32-x64、linux-arm64），与操作系统+架构组合一一对应。