主进程支持特殊启动模式：`--gui-schedule-mcp-server` / `--claw-schedule-mcp-server` 时仅运行调度 MCP 服务器并隐藏 Dock；渲染器通过 URL 查询参数 `storageRelocation=1`、`runtimeMigrationRecovery=1` 进入专用引导视图；开发模式下由 `devServerHintUrl` 决定加载 dev server URL 而非打包产物。