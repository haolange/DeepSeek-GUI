Electron + TypeScript + React（Vite 构建渲染器），使用 `@xyflow/react` 绘制流程图；通过 `node:crypto`、`node:path`、`node:url` 等 Node 内置模块操作文件系统；IPC 消息体使用 JSON Schema（`main/ipc/app-ipc-schemas`）进行类型校验；运行时通过 HTTP SSE 与 Kun 共享进程通信。