`@kun/extension-api` 在 `prepack` 钩子中执行 `npm run schema:check`，即先构建再运行 `scripts/generate-manifest-schema.mjs --check` 校验 manifest schema 一致性；其他包遵循统一的 `build → tsc -p tsconfig.build.json`、`test → vitest run --root .` 脚本约定。