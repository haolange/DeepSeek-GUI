- 所有面向用户的主题文档均维护中英文双版本，英文文件以 `.en.md` 后缀与中文版并列存放于同一目录。
- 文档内部通过相对路径相互链接（如 `./kun-architecture.md`、`../README.md`），避免使用绝对路径或外部域名引用。
- 贡献指南与 PR 模板约定使用 Conventional Commits（feat/fix/docs/refactor/style/chore）并遵循 Summary/Why/Validation/Media/Tests 的 PR 结构。
- 截图与演示图片统一存放在 `assets/readme/` 与 `screenshots/<feature>/` 下，并按语义化命名（如 `01-settings-entry.png`、`code-workspace-demo.webp`）。